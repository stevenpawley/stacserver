% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signers.R
\name{azure_signer}
\alias{azure_signer}
\title{Create a reusable Azure Blob Storage signing function}
\usage{
azure_signer(
  endpoint = Sys.getenv("AZURE_STORAGE_ENDPOINT"),
  expiry_seconds = 3600L,
  key_lifetime_seconds = 24L * 3600L,
  token = NULL
)
}
\arguments{
\item{endpoint}{Full blob service URL, e.g.
\code{"https://myaccount.blob.core.windows.net/"}. Defaults to the
\code{AZURE_STORAGE_ENDPOINT} environment variable.}

\item{expiry_seconds}{Lifetime of each signed URL in seconds (default 3600).}

\item{key_lifetime_seconds}{How long each cached user delegation key is
requested for (default 24 hours). Azure caps this at seven days, and it
must exceed \code{expiry_seconds}, otherwise every signature would need a fresh
key and the cache would buy nothing.}

\item{token}{An Azure AD token from \code{\link[AzureAuth:get_managed_token]{AzureAuth::get_managed_token()}} or
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}. Defaults to \code{NULL}, meaning a managed
identity token is obtained when a key is needed — the usual choice on
Azure-hosted infrastructure. For service principal auth, obtain a token
with \code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} and pass it here; a supplied token is
refreshed when it goes stale.}
}
\value{
A function of one argument (\code{href}) returning a signed URL. Call it
directly to sign a single href: \code{azure_signer()(href)}.
}
\description{
Returns a \verb{function(href)} suitable for the \code{sign_fn} argument of
\code{\link[=stac_api_router]{stac_api_router()}}. The returned function fetches the user delegation key
\strong{once} and reuses it for every href it signs, refreshing only when the key
is close enough to expiry that it could no longer cover a new signature.
}
\details{
This matters for a server. Minting a user delegation key is a network round
trip to Azure, and the router signs every asset href in every response, so
an items page holding ten items with four assets apiece would otherwise make
forty such calls before it could reply. Computing the SAS itself is local,
so only the key needs caching.

Each signature is read-only, scoped to the single blob it names, valid for
\code{expiry_seconds}, and restricted to HTTPS. An href that does not point at
\code{endpoint} is returned unchanged, so a catalog holding a mixture of private
blobs and public URLs is left alone where it should be.
}
\examples{
\dontrun{
router <- stac_api_router(
  con,
  base_url = "https://stac.example.com",
  sign_fn = azure_signer(expiry_seconds = 3600)
)
}
}
