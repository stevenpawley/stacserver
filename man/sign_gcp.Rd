% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signers.R
\name{sign_gcp}
\alias{sign_gcp}
\title{Sign a Google Cloud Storage href using Application Default Credentials.}
\usage{
sign_gcp(href, expiry_seconds = 3600L)
}
\arguments{
\item{href}{Unsigned GCS URL. Accepts both \verb{gs://bucket/object} and
\verb{https://storage.googleapis.com/bucket/object} forms.}

\item{expiry_seconds}{Lifetime of the signed URL in seconds (default 3600).}
}
\value{
A signed URL string.
}
\description{
Generates a short-lived V4 signed URL for a GCS object. Authentication is
handled by \code{googleCloudStorageR} / \code{googleAuthR} — call
\code{googleCloudStorageR::gcs_auth()} (or set \code{GOOGLE_APPLICATION_CREDENTIALS})
before use. On GCE the metadata server is used automatically. Suitable for
passing directly as the \code{sign_fn} argument of \code{\link[=stac_api_router]{stac_api_router()}}.
}
