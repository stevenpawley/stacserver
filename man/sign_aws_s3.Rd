% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signers.R
\name{sign_aws_s3}
\alias{sign_aws_s3}
\title{Sign an AWS S3 href using a presigned URL.}
\usage{
sign_aws_s3(
  href,
  expiry_seconds = 3600L,
  region = Sys.getenv("AWS_DEFAULT_REGION", unset = "us-east-1")
)
}
\arguments{
\item{href}{Unsigned S3 URL. Accepts \verb{s3://bucket/key}, virtual-hosted
style (\verb{https://bucket.s3.region.amazonaws.com/key}), and path style
(\verb{https://s3.region.amazonaws.com/bucket/key}).}

\item{expiry_seconds}{Lifetime of the presigned URL in seconds
(default 3600).}

\item{region}{AWS region. Defaults to the \code{AWS_DEFAULT_REGION} environment
variable, falling back to \code{"us-east-1"}.}
}
\value{
A presigned URL string.
}
\description{
Generates a short-lived presigned GET URL for an S3 object using
\code{paws.storage}. Authentication follows the standard AWS credential chain:
environment variables (\code{AWS_ACCESS_KEY_ID}, \code{AWS_SECRET_ACCESS_KEY},
\code{AWS_SESSION_TOKEN}), \verb{~/.aws/credentials}, or an IAM instance profile
(EC2, ECS, Lambda). Suitable for passing directly as the \code{sign_fn}
argument of \code{\link[=stac_api_router]{stac_api_router()}}.
}
